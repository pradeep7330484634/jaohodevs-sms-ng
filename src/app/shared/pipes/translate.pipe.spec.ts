import { TranslatePipe } from './translate.pipe';

describe('TranslatePipe', () => {
  it('create an instance', () => {
    // const pipe = new TranslatePipe(useLang('en'));
    // expect(pipe).toBeTruthy();
  });
});
